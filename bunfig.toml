telemetry = false

[install]
# Optimize package installation for monorepo speed
smol = true
exact = true

[test]
passWithNoTests = true

# Keep coverage opt-in on the CLI (`bun test --coverage`).
# The repo-level threshold gate lives in scripts/check-coverage.ts.
# coverage = true
