{
    "name": "@gobing-ai/typescript-bun-starter",
    "version": "0.1.3",
    "publishConfig": {
        "access": "public"
    },
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "check": "biome check . && bun run check:contracts && bun run typecheck && bun run test:coverage",
        "check:contracts": "bun run scripts/check-contracts.ts",
        "test": "bun test --coverage --coverage-reporter=lcov --coverage-dir=coverage && bun run scripts/check-coverage.ts",
        "test:coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov --coverage-dir=coverage --reporter=dots && bun run scripts/check-coverage.ts",
        "format": "biome format --write .",
        "lint-fix": "biome lint --write .",
        "typecheck": "tsc --noEmit",
        "db:push": "drizzle-kit push",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "drizzle-kit migrate",
        "dev:cli": "bun run apps/cli/src/index.ts",
        "dev:server": "bun --hot run apps/server/src/index.ts",
        "dev:web": "cd apps/web && npx astro dev",
        "dev:all": "bun run dev:server & bun run dev:web",
        "build:web": "cd apps/web && npx astro build",
        "build:cli": "bun build --compile apps/cli/src/index.ts --outfile dist/tbs",
        "tree": "tree . -a -I 'node_modules|.git|coverage'",
        "pub2npmjs": "npm whoami >/dev/null 2>&1 || { echo 'Error: not logged in to npm. Run `npm login` first.'; exit 1; } && npm publish --access public",
        "clean-demo": "bun run scripts/clean-demo.ts"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.0.0",
        "@types/bun": "^1.3.12",
        "better-sqlite3": "^12.8.0",
        "drizzle-kit": "^0.31.0",
        "typescript": "^5.7.0"
    }
}
