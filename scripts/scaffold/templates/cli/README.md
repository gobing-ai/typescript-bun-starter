# CLI Template

This directory should contain the template files for the CLI workspace.

To populate, run:
```bash
bun run generate:scaffold-templates
```

Or manually place the baseline CLI files here preserving the directory structure:
- `apps/cli/src/index.ts`
- `apps/cli/src/config.ts`
- `apps/cli/package.json`
