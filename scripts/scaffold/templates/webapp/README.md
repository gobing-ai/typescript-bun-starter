# WebApp Template

This directory should contain the template files for the web workspace.

To populate, run:
```bash
bun run generate:scaffold-templates
```

Or manually place the baseline web files here preserving the directory structure:
- `apps/web/src/pages/index.astro`
- `apps/web/package.json`
